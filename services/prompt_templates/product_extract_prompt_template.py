"""
产品结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "product_extract_prompt_template"

# 检索问题/抽取主题默认值
q = "提取{company_name}近三年的产品生产情况"

# system 提示词
system_prompt = "你是一个数据提取分析师，基于提供的文本，提取出其中近三年的数值信息和关联数值信息，输出数据列表。"

# user 提示词模板（必须包含 {text} 占位符）
user_prompt = """
任务：
    你需要从提供的文本中抽取相关信息，并输出一个JSON对象。

产品产量可能包含字段：
    名称 （列如：品名、种类，产品名称等）
    年份
    产品数量
    数量单位
    产值
    产值单位
    工业增加值
    工业增加值单位

要求：
    - 不要解释
    - 不要markdown
    - 不要换行格式化
    - 缺失字段为null

文本：
{text}
""".strip()

