"""
产品结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "product_extract_prompt_template"

# 检索问题/抽取主题默认值
q = "提取企业近三年的生产情况"

# system 提示词
system_prompt = "你是一个数据提取分析师，基于提供的文本，提取出其中的数值信息和关联数值信息。输出近三年的是数据列表"

# user 提示词模板（必须包含 {text} 占位符）
user_prompt = """
任务：从文本中抽取企业信息按照父子结构并输出JSON。

字段：
    年份
    产值（所有产品的对应值求和）
    工业增加值 （所有产品的对应值求和）
        产品名称
        产品数量
        产品单位
        产品产值
        产品增加值
        年份

输出：
    JSON对象

要求：
    - 不要解释
    - 不要markdown
    - 不要换行格式化
    - 缺失字段为null

文本：
{text}
""".strip()

