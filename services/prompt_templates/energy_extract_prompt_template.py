"""
能源结构化抽取配置（单文件三变量）。
"""

SERVICE_NAME = "energy_extract_prompt_template"

# 检索问题/抽取主题默认值
q = "提取企业能源消耗情况、能源利用情况、能源节约情况"

# system 提示词
system_prompt = "你是一个数据提取分析师，基于提供的文本，提取出其中能源消耗近三年的数值信息和关联数值信息。"

# user 提示词模板（必须包含 {text} 占位符）
user_prompt = """
任务：从文本中抽取企业信息并输出JSON。

字段：
    年度
    综合能耗当量值 （求和）
    综合能耗等价值  (求和)
        能源品种
        实物量
        单位
        当量值折标系数
        当量值
        等价值折标系数
        等价值

输出：
    JSON对象

要求：
    - 不要解释
    - 不要markdown
    - 不要换行格式化
    - 缺失字段为null

文本：
{text}
""".strip()

